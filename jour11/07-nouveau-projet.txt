npm create vite jour11-blog 
React 
Javascript 

cd jour11-blog 
npm i 
npm i react-router-dom 
npm i firebase 
npm run dev

=> ajouter dans la page d'accueil un formulaire 
qui contient 2 valeurs champs 

input titre 
textarea pour le contenu 
bouton de soumission

ce formulaire n'est visible QUE site vous vous être authentifié correctement au préalable 

une fois avoir vérifier que toutes les informations sont correctes => INSERT le données saisie dans une collection articles via l'instruction asynchrone addDoc
